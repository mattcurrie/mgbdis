# Initial WRAM/HRAM Recovery Notes

This document tracks memory-map recovery for `Yoshi/yoshi.gb`. Confidence levels:

- **High**: direct code behavior and existing labels agree.
- **Medium**: strong contextual evidence, but name may still be refined.
- **Low**: useful hypothesis only.

## Cartridge / ROM Banking

| Address / Value | Name | Confidence | Evidence |
|-----------------|------|------------|----------|
| `$2100` | `MBC1_ROM_BANK_REG` | High | Every observed ROM bank switch writes here; the ROM header declares cartridge type `$01` (MBC1). |
| `$01` | `ROM_BANK_MAIN_CODE` | High | Restored after graphics loads; contains VBlank, sprites, sound, and normal cross-bank routines. |
| `$02` | `ROM_BANK_GRAPHICS_0` | High | Selected for Bank 2 title/game/common/two-player graphics copies. |
| `$03` | `ROM_BANK_GRAPHICS_1` | High | Selected for Bank 3 match/result/high-score graphics copies. |

## Existing Named HRAM

These definitions already exist in `Yoshi/constants.inc` and are referenced by the disassembly.

| Address | Name | Confidence | Evidence |
|---------|------|------------|----------|
| `$FF80` | `OAM_DMA_HRAM` | High | Startup clears HRAM through `ClearHRAMWorkAreaLoop`, then `SetupOAMDMA` copies the OAM DMA routine to this address; `VBlankHandler` calls it after `VRAMCopyDMA`. |
| `$FF8A` | `ANIM_FRAME` | Medium | Heavy use in matching/result/title animation paths; may be a generic temporary animation counter rather than one semantic variable. |
| `$FF8B` | `STATE_TRANSITION` | Medium | Used as a transition/phase flag in matching, BG update, next-piece drawing, round transitions. |
| `$FF8C` | `ANIM_SUBFRAME` | Medium | Used near result/link/title animation paths. |
| `$FF8D` | `UI_SCRATCH` | Medium | Shared temporary byte used by UI/result paths: piece-display occupancy counting, pre-play level text indexing, result-record leading-zero suppression, result rank/result-code staging, and link confirm panel tile alternation. |
| `$FF93` | `SCREEN_STATE` | Medium | Used in scan/timer/result/menu code; exact semantics still need validation. |
| `$FF94` | `TITLE_RESET_UNUSED_HRAM_FLAG` | Low | Set to `1` by `ResetTitleState`; no consumer has been confirmed in the current Yoshi source. |
| `$FF97-$FF9A` | `PIECE_DISPLAY_SLOT_ORDER` | High | Four slot indices initialized to `0,1,2,3`, shuffled by `ShufflePieceDisplaySlotOrder`, and used by `BuildPieceDisplayStatesForCount` to choose which `PIECE_DISPLAY_STATES` entry to write. |
| `$FF9B` | `WAVE_UPDATE` | High | Checked by VBlank before wave RAM update. |
| `$FF9C` | `SCX_SHADOW` | High | Copied to `rSCX` in VBlank. |
| `$FF9D` | `SCY_SHADOW` | High | Startup clears it and Bank 1 VBlank copies it to `rSCY`. |
| `$FF9E` | `WY_SHADOW` | High | Startup initializes it to `WY_OFFSCREEN_Y` (`SCRN_Y`, `$90`) and Bank 1 VBlank copies it to `rWY`. |
| `$FFA0` | `JOYPAD_RAW` | High | Written by `ReadJoypadButtons`; consumed as input state. The poll uses `P1F_GET_DPAD`, `P1F_GET_BTN`, `P1F_GET_NONE`, and `P1_INPUT_BITS_MASK` around `rP1`; Bank 1 `CheckJoypadRaw` uses the same constants for its direct button-line read. |
| `$FFA1` | `JOYPAD_PRESSED` | High | Written by `ReadJoypadButtons`; read by menu/game/pause handlers. `WaitAnyButtonPress` and `PollResultRecordBlinkInput` mask it with `PADF_ANY_BUTTON` to wait for A/B/Select/Start while ignoring D-pad presses; the static-dead Bank 1 Select helper uses `PADF_SELECT`, `PADF_START_OR_SELECT`, and `PADF_SELECT_CLEAR_MASK`. |
| `$FFA2` | `JOYPAD_HELD` | High | Written by `ReadJoypadButtons`; read by joypad helpers. `ResetJoypadStateAndReinitOnRelease` clears it before `WaitJoypadLinesReleasedLoop` polls the raw joypad lines until they recover, then jumps back to `Init`; the released low nibble is named `P1_INPUT_BITS_RELEASED`. |
| `$FFA5` | `BG_MAP_SHADOW_COPY_ENABLE_FLAG` | High | `CopyNextBgMapShadowSlice` is the only confirmed reader. When nonzero, it copies one six-row `BG_MAP_SHADOW` slice to BG map VRAM during VBlank. `StateInit`, `ProcessMatching`, and the round-complete summary setup enable it after preparing shadow tilemap content; the round-complete summary setup temporarily clears it while rewriting the shadow map and scheduling direct VRAM tile transfers. |
| `$FFA6` | `BG_MAP_COPY_PHASE` | High | `CopyNextBgMapShadowSlice` rotates this through 0, 1, and 2 to copy `BG_MAP_SHADOW` to BG map VRAM in three six-row slices while `BG_MAP_SHADOW_COPY_ENABLE_FLAG` is nonzero. |
| `$FFA7-$FFA8` | `VBLANK_SAVED_SP_HI` / `VBLANK_SAVED_SP_LO` | High | Both pop-based VBlank copy loops save the caller SP here, temporarily load SP from a copy source, then restore SP before returning. |
| `$FFAE` | `VRAM_COPY_BLOCKS` | High | `VRAMCopySetup` writes a length/count byte here, capped at `VRAM_COPY_MAX_BLOCKS_PER_VBLANK`; `VRAMCopyDMA` reads it as a loop count and transfers 16 bytes per iteration. |
| `$FFAF-$FFB0` | `VRAM_SRC_LO`/`VRAM_SRC_HI` | High | `VRAMCopySetup` stores the source pointer here; `VRAMCopyDMA` loads it into SP before popping 16-byte chunks. |
| `$FFB1-$FFB2` | `VRAM_DST_LO`/`VRAM_DST_HI` | High | `VRAMCopySetup` stores destination pointer here; `VRAMCopyDMA` updates this pair after the copy loop. |
| `$FFB3-$FFB7` | `UNUSED_VRAM_COPY2_*` | High | Written only by the unreachable `UnusedVRAMCopy2Setup` fragment at `00:$0244`; no VBlank/DMA consumer reads this secondary slot. |
| `$FFBB-$FFC3` | `RNG_STATE` / `RNG_WORK` / `RNG_MULTIPLIER_LOW_WORK` | High | `Multiply` updates the four-byte `RNG_STATE` by seeding `RNG_WORK` with `RNG_INCREMENT_BYTE_0..3`, shifting the state through `RNG_MULTIPLIER_HIGH_WORD:RNG_MULTIPLIER_LOW_BYTE`, copying the work bytes back to state, and returning the third work byte in `A`. |
| `$FFC4` | `VBLANK_BUSY` | Medium | Used by VBlank sync/wait logic. |
| `$FFC5` | `VBLANK_SYNC` | Medium | `WaitVBlank` writes `VBLANK_SYNC_REQUESTED`; Bank 1 VBlank clears the byte after observing it. |
| `$FFC7` | `GAME_STATE` | High | Main state machine index in `MainLoop`. |
| `$FFC8` | `SERIAL_DONE` | High | Serial transfer completion flag; `FinishSerialInterrupt` stores `SERIAL_DONE_ACTIVE`, and the preplay/settings/result wait paths clear it after consuming the received byte. |
| `$FFE0` | `SERIAL_TEMP` | Low | Existing name; current reference scan shows mostly temporary writes and needs validation. |

## Existing Named WRAM

| Address | Name | Confidence | Evidence |
|---------|------|------------|----------|
| `$C4A0-$C607` | `BG_MAP_SHADOW` | High | `FillGameTilemap` / `FillTitleTilemap` share `BeginBgMapShadowFill` and `FillBgMapShadowLoop` to fill `$0168` bytes with `GAME_BG_SHADOW_CLEAR_TILE` / `TITLE_BG_SHADOW_CLEAR_TILE`; `CalcTilemapAddress` maps row/column pairs as `$C4A0 + row * 20 + column` through explicit carry steps (`AddTilemapColumnOffset`, `AddBgMapShadowBaseLow`, `StoreCalculatedTilemapAddressLow`), and Bank 1 copies three six-row slices from `$C4A0/$C518/$C590` to BG map VRAM `$9C00/$9CC0/$9D80`. |
| `$C4B1/$C4B5/$C4C5` | `TITLE_FRAME_TOP_RIGHT_CAP_TOP_LEFT` / `TITLE_FRAME_INNER_TOP_LEFT` / `TITLE_FRAME_RIGHT_STRIP_TOP_LEFT` | Medium | `InitTitleUI` fills these fixed title-screen frame rectangles with tile bases `$50`, `$80`, and `$C0`. |
| `$C4B7/$C4C3` | `LINK_RESULT_LEFT_BADGE_TOP_LEFT` / `LINK_RESULT_RIGHT_BADGE_TOP_LEFT` | Medium | `UpdateLinkResultMarksAndScreen` fills two 2x2 2P result badge boxes here with tile bases swapped by `LINK_ROLE`. |
| `$C4CD/$C4D3` | `LINK_RESULT_LEFT_HEADER_TOP_LEFT` / `LINK_RESULT_RIGHT_HEADER_TOP_LEFT` | Medium | The same 2P result setup fills two one-row, four-tile header blocks here with tile bases swapped by `LINK_ROLE`. |
| `$C4C8` | `FIELD_OCCUPANCY_SCAN_TOP_LEFT` | Medium | `CountFieldOccupancyIntoUiScratch` scans this 7x4 sample through `FieldOccupancyScanRowLoop` / `FieldOccupancyScanColumnLoop`, and `QueueLinkFieldOccupancyCount` scans the same region, counting tiles that are not `FIELD_OCCUPANCY_EMPTY_TILE` (`$4A`). |
| `$C4CA/$C4ED/$C59B` | `MATCHING_*` layout origins | Medium | `ProcessMatching` clamps `STATE_TRANSITION` to `MATCHING_LAST_STATE`, clears BG map VRAM and `BG_MAP_SHADOW` with `MATCHING_BG_CLEAR_TILE`, fills the intro blink block at `$C4ED` through `DrawMatchingIntroBlinkBlock`, updates the result header at `$C4CA`, and writes a three-tile animation strip at `$C59B` while moving the matching OAM pair. |
| `$C507/$C510/$C575` | `TITLE_MENU_PANEL_TOP_LEFT` / `TITLE_LEVEL_STRIP_TOP_LEFT` / `TITLE_BOTTOM_RIGHT_PANEL_TOP_LEFT` | Medium | `InitTitleUI` fills these fixed title-screen panel/strip rectangles with tile bases `$D0`, `$70`, and `$34`. |
| `$C509` | `RESULT_LEVEL_LABEL_TOP_LEFT` | Medium | `DrawMatchingResultStats` fills `RESULT_LABEL_RECT_SIZE` with `RESULT_LEVEL_LABEL_TILE` here immediately before drawing `LEVEL_DISPLAY_TENS` / `LEVEL_DISPLAY_ONES`. It is `BG_MAP_SHADOW + row 5 * $14 + column 5`. |
| `$C50F-$C510` | `RESULT_LEVEL_VALUE_TENS` / `RESULT_LEVEL_VALUE_ONES` | High | `DrawMatchingResultStats` writes `LEVEL_DISPLAY_ONES + RESULT_DIGIT_TILE_BASE` at `$C510`, then `LEVEL_DISPLAY_TENS + RESULT_DIGIT_TILE_BASE` at `$C50F`. `TITLE_LEVEL_STRIP_TOP_LEFT` gives the same `$C510` address its title-screen layout role. |
| `$C511-$C512` | `RESULT_SPEED_VALUE_TOP_LEFT` | Medium | `DrawMatchingResultStats` draws the two-tile speed value from `ACTIVE_SPEED * 2 + RESULT_SPEED_TILE_BASE` here. It is `BG_MAP_SHADOW + row 5 * $14 + column 13`. |
| `$C4B5` | `RESULT_RECORD_SCREEN_HEADER_TOP_LEFT` | Medium | Result record setup fills a two-row, 18-tile header rectangle here with tile `$00` after clearing `BG_MAP_SHADOW`. The same address can also be a title-frame origin in the title setup context. |
| `$C4F7/$C4F8` | `RESULT_RECORD_TYPE_LABEL_TOP_LEFT` / `RESULT_RECORD_B_TYPE_LABEL_PATCH` | Medium | Result record setup fills the type label from `$C4F7`; B-type patches the second label tile at `$C4F8` to `$2A`. `$C4F7` also overlaps a 2P result mark origin in the 2P result context. |
| `$C504` | `RESULT_RECORD_BOX_TOP_LEFT` | High | Result record setup calls `FillResultRecordBoxRow` from this origin to draw the top, six body rows, and bottom row of the record frame. |
| `$C51D/$C527` | `RESULT_RECORD_COLUMN_HEADER_TOP_LEFT` / `RESULT_RECORD_B_TYPE_HEADER_PATCH_TOP_LEFT` | High | The record column header is filled from `$C51D`; B-type overwrites a three-tile section at `$C527`. |
| `$C52D/$C555/$C57D` | `RESULT_RECORD_LABEL_ORIGIN_0..2` | High | Result setup fills three 3-tile record labels here; `WaitResultRecordScreenInput` blinks one selected row between normal and selected tile sets using `RESULT_RECORD_LABEL_BLINK_ALT_START_FRAME` and `RESULT_RECORD_LABEL_BLINK_PERIOD`. |
| `$C530` | `RESULT_RECORD_VALUE_TOP_LEFT` | High | `DrawStoredResultRecords` renders the three result records starting here: five score digits, two level digits, and either three A-type detail digits or two B-type timer digit pairs separated by `RESULT_RECORD_TIMER_SEPARATOR_TILE`. The digit helper masks with `RESULT_RECORD_DIGIT_MASK`, adds `RESULT_RECORD_DIGIT_TILE_BASE`, and steps to the next rendered row with `RESULT_RECORD_NEXT_RENDER_ROW_DELTA` after each variable-width row. |
| `$C534/$C538/$C53D` | `RESULT_RECORD_SCORE_PLACEHOLDER_ORIGIN` / `RESULT_RECORD_LEVEL_PLACEHOLDER_ORIGIN` / `RESULT_RECORD_DETAIL_PLACEHOLDER_ORIGIN` | High | `FillResultRecordPlaceholderColumn` seeds placeholders down all three result rows before `DrawStoredResultRecords` overwrites them with stored record digits where records exist. |
| `$C4A0-$C5EF`, `$C4B4/$C544/$C56A` | `ROUND_COMPLETE_SUMMARY_BG_FILL_*` and `ROUND_COMPLETE_SUMMARY_HEADER/PANEL/MESSAGE_*` | High | `ShowATypeRoundCompleteSummary` temporarily disables BG-map shadow copying, fills a 16x16 shadow-tilemap region from `$C4A0` with `ROUND_COMPLETE_SUMMARY_BG_FILL_TILE`, then draws a 16-tile top strip at `$C4B4`, a 2x8 panel at `$C544`, and copies one 12-byte summary message to `$C56A` before revealing the four round-complete boxes. |
| `$C546` | `RESULT_MAIN_PANEL_TOP_LEFT` | Medium | Repeated `FillRect` origin for the central result/matching panel. It is `BG_MAP_SHADOW + row 8 * $14 + column 6`, used by `ProcessMatching` and `PlayConfirmSound` with 6-row fills. `ProcessMatching` uses `RESULT_MAIN_PANEL_RECT_SIZE` and `RESULT_MAIN_PANEL_WITH_EDGE_RECT_SIZE` while stepping through the matching result-panel fill stages. |
| `$C54C` | `RESULT_MAIN_PANEL_RIGHT_EDGE` | Medium | `ProcessMatching` writes `MATCHING_BG_CLEAR_TILE` down `RESULT_MAIN_PANEL_EDGE_ROWS` rows from this origin through `FillResultMainPanelRightEdgeLoop` using `BG_MAP_ROW_STRIDE`; it is the right-edge column of the `RESULT_MAIN_PANEL_TOP_LEFT` area. |
| `$C565-$C566` | `LINK_LOCAL_FIELD_COUNT_TENS` / `LINK_LOCAL_FIELD_COUNT_ONES` | Medium | `QueueLinkFieldOccupancyCount` draws the local non-empty field sample count here as two tile digits and queues the same count with `LINK_FIELD_COUNT_PACKET_FLAG` / `LINK_FIELD_COUNT_PACKET_BIT`. |
| `$C572/$C574` | `LINK_RESULT_OUTCOME_LEFT_TOP_LEFT` / `LINK_RESULT_OUTCOME_RIGHT_TOP_LEFT` | Medium | The terminal 2P result branch fills one of these two 4x3 outcome blocks with tile bases `$D7/$E3`, selected by `LINK_ROLE` and `ANIM_FRAME`. |
| `$C543/$C571` | `LINK_RESULT_WAIT_PANEL_TOP_LEFT` / `LINK_RESULT_CONFIRM_DETAIL_TOP_LEFT` | Medium | Link result confirmation fills a 6x6 wait panel at `$C543`; `PlayConfirmSound` optionally fills a 4x3 non-master detail block at `$C571`. |
| `$C5D0/$C5D7` | `LINK_RESULT_STATUS_TOP_LEFT` / `LINK_RESULT_BOTTOM_TEXT_TOP_LEFT` | Medium | `DrawLinkResultRoleStatusStrip` and the terminal 2P result path reuse the row-15 status/text strip, clearing it with tile `$39` or drawing role-dependent tile bases `$19/$23`. |
| `$C5D1` | `RESULT_SCORE_LABEL_TOP_LEFT` | Medium | `DrawMatchingResultStats` fills `RESULT_LABEL_RECT_SIZE` with `RESULT_SCORE_LABEL_TILE` here immediately before drawing `RESULT_SCORE_DIGIT_COUNT` score digits at `RESULT_SCORE_VALUE_TOP_LEFT`. It is `BG_MAP_SHADOW + row 15 * $14 + column 5`. |
| `$C5D6` | `RESULT_SCORE_VALUE_TOP_LEFT` | Medium | `DrawMatchingResultStats` writes five rendered `SCORE_DIGITS` here through `DrawResultScoreDigitsLoop`, masking each byte with `RESULT_DIGIT_MASK` and adding `RESULT_DIGIT_TILE_BASE`; result fill helpers also clear/fill this value area. It is `BG_MAP_SHADOW + row 15 * $14 + column 10`. |
| `$C5DD-$C5DE` | `LINK_PEER_FIELD_COUNT_TENS` / `LINK_PEER_FIELD_COUNT_ONES` | Medium | `ProcessLinkFieldCountPacket` clears `LINK_FIELD_COUNT_PACKET_BIT` and draws incoming field-count packets here as two tile digits; `ProcessLinkResultPacket` clears both digits to tile `$40` when queueing a zero result without local game-over. |
| `$C5B9/$C5BD/$C5C1/$C5C5` | `ROUND_COMPLETE_TILEMAP_ORIGIN_0..3` | High | Round-complete setup fills four 2x2 boxes with `ROUND_COMPLETE_TILEMAP_PENDING_TILE`; `ShowRoundComplete` reveals each box, emits the corresponding round-complete sprite group with base X `$10/$30/$50/$70`, then clears the box. |
| `$C5A6/$C5B0/$C5DA` | `RESULT_RECORD_A/B_TYPE_DETAIL_*` | High | Result record setup draws A-type detail panels at `$C5A6/$C5B0`; B-type reuses `$C5A6` for the wider detail panel and adds a 2x2 mark at `$C5DA`. |
| `$C5E5` | `RESULT_TIME_LABEL_TOP_LEFT` | Medium | `DrawMatchingResultStats` fills `RESULT_TIME_LABEL_RECT_SIZE` with `RESULT_TIME_LABEL_TILE` here immediately before drawing elapsed-time digits. It is `BG_MAP_SHADOW + row 16 * $14 + column 5`. |
| `$C5EA` | `RESULT_TIME_VALUE_TOP_LEFT` | Medium | `DrawMatchingResultStats` writes four rendered `ROUND_TIMER_DIGITS` here with `RESULT_TIME_SEPARATOR_TILE` between the second and third digits. It is `BG_MAP_SHADOW + row 16 * $14 + column 10`. |
| `$C61C` | `LCD_REDRAW` | High | Read by `UpdateSprites`, written around LCD/VRAM refresh paths. `LCD_REDRAW_EXPAND_REQUEST` expands logical sprite objects, while `LCD_REDRAW_HIDE_ALL_REQUEST` stores `LCD_REDRAW_HIDE_ALL_SENTINEL` and hides all sprites; matching/result setup paths use `xor a` for the hide-all request to preserve the original byte shape. |
| `$C61D-$C61F` | `SCORE_BCD_LOW` / `SCORE_BCD_MID` / `SCORE_BCD_HIGH` | High | Bank 1 `AddScore` adds an `HL` BCD score delta with `daa`, caps overflow at `99999` through `SCORE_BCD_*_MAX`, and stores the packed BCD accumulator here. |
| `$C620` | `SCORE_PRESERVED_UNUSED_BYTE` | Low | `ResetTitleState` sets it to `1`; `ResetScoreAccumulatorAndDigits` saves it, clears `SCORE_CLEAR_BYTE_COUNT` bytes from `SCORE_BCD_LOW`, then restores this byte. No consumer has been confirmed. |
| `$C621-$C625` | `SCORE_DIGITS` | High | `AddScore` unpacks the BCD score into `SCORE_DISPLAY_DIGIT_COUNT` low-nibble display digits; Bank 0 `DrawMatchingResultStats` reads five bytes from this range when drawing the score. |
| `$C628-$C629/$C672` | `SCORE_UNUSED_TILE_BASE_*` | Low | `InitBTypeFallTimingAndBoardSeed` seeds `$C672` with `SCORE_UNUSED_TILE_BASE_INITIAL` (`$30`); `AddScore` copies it to `$C629` and the swapped value to `$C628`. No independent consumer has been confirmed. |
| `$C62A-$C669` | `BOARD_DATA` | High | Four 16-byte column blocks. `ClearBoardData` / `ClearBoardDataLoop` clear `$40` bytes, `DrawAllColumns` renders seven visible entries per column from `BOARD_DATA + BOARD_CELL_VISIBLE_PAYLOAD_OFFSET`, `BOARD_COLUMN_BOTTOM_VISIBLE_CELL` marks the bottom visible odd offset at `BOARD_DATA + $0D`, and `ReadBoardCellAtColumnRow` / `StagePiecePayloadInSelectedColumn` index a column as `FALLING_PIECE_GRID_COLUMN * $10` plus row/fall position. Visible payloads draw as 4x2 tile blocks through `DrawGridPiece`; the paired even byte is `BOARD_CELL_UNREAD_PAIR_OFFSET` because no live piece/display/scan consumer has been confirmed. |
| `$C66A-$C66D` | `COLUMN_TOP_ROWS` | High | Four per-column row/fall-target bytes. Seeded from level setup, indexed by `FALLING_PIECE_GRID_COLUMN`, used by drawing and fall logic, and swapped by `AnimateDropping`; `BOARD_FALL_END_ROW` (`$0F`) is the clear/end boundary. |
| `$C66E` | `SPRITE_OBJECT_DELAY_RELOAD` | High | Initialized to `1` by `ResetTitleState` and copied into `SPRITE_OBJECT_STAGING + SPRITE_OBJECT_DELAY_COUNTER` by `TickSpriteObjectWaitPhase` when `UpdateSpriteObject` advances a waiting gameplay object slot. |
| `$C66F` | `DROP_CURSOR_ANIM_ACTIVE` | High | Set to `DROP_CURSOR_ANIM_ACTIVE_VALUE` when `HandlePlayfieldInput` accepts a drop input, drives `UpdateDropCursorAnimation`, and gates `UpdatePieceFallTimer` timing. |
| `$C670` | `DROP_CURSOR_FRAME_TIMER` | High | Decremented and reloaded by `UpdateDropCursorAnimation` before advancing `SPRITE_OBJECT_SLOT_0 + SPRITE_OBJECT_FRAME`. |
| `$C671` | `GAME_TYPE` | High | Set from `OPTION_GAME_TYPE` in 1P and forced to `GAME_TYPE_B` in 2P; selects A/B-style gameplay/layout/result behavior, not 1P/2P. |
| `$C673-$C677` | `PIECE_DISPLAY_CODE_POOL` | High | Five code bytes initialized from `PIECE_DISPLAY_CODE_FIRST` by `InitPieceDisplayCodePoolLoop`; `PIECE_DISPLAY_SHUFFLE_INDEX_MASK` selects indices `0..3` for the shuffle helpers, `BuildPieceDisplayStatesForCount` indexes this pool before `SelectPieceDisplayCode`, and initial B-type board fill reads `INITIAL_BOARD_PIECE_POOL_OFFSET` before the adjacent-match wrap check. |
| `$C68F` | `PIECE_FALL_POS` | Medium | Incremented by `AdvanceFallingPiecePosition`, compared with the selected column top row, and used by scan/landing routines. |
| `$C691` | `FALLING_PIECE_GRID_COLUMN` | High | This is `SPRITE_OBJECT_STAGING + SPRITE_OBJECT_GRID_COLUMN`, copied from the active gameplay sprite slot by `UpdateSpriteObject`. It indexes `COLUMN_TOP_ROWS`, selects the 16-byte `BOARD_DATA` column block, chooses the field-column effect slot/timer, and positions round-transition/link field effects. |
| `$C69D` | `UNRESOLVED_LANDING_RESET_BYTE_0` | Low | Cleared by `ClearRoundLandingAndResultState` and again by `HandleMatchedLandingScanState` when `UNRESOLVED_LANDING_SCAN_COUNTER` reaches zero; no consumer has been confirmed. |
| `$C697` | `PIECE_DISPLAY_REMAINING` | Medium | `LoadGameTurnPieceDisplayStep` stores the same value as `PIECE_DISPLAY_COUNT`; `StagePiecePayloadInSelectedColumn` and `DecrementPieceDisplayRemaining` decrement it, but no independent read has been confirmed. |
| `$C698` | `PIECE_DISPLAY_COUNT` | High | Loaded from the second byte of the current `GameTurnParamTable` row or forced to `2` during gameplay setup; callers pass it into `BuildPieceDisplayStatesForCount`. |
| `$C699` | `COLUMN_TOP_ROW_SEED` | High | Loaded from `BTypeColumnTopRowSeedTable` or forced to `BOARD_FALL_END_ROW`; `SeedColumnTopRows` copies it into all four `COLUMN_TOP_ROWS` entries, and `FillInitialBoardColumns` uses it to size the initial board-fill loop. |
| `$C6A2` | `BOARD_SCAN_REWARD_INDEX` | High | `RunBoardScanRoundTransition` saves the pre-remap `SCREEN_STATE` here before selecting the transition frame limit from `BoardScanTransitionFrameLimitTable`; `StoreBoardScanDistanceRewardIndex` can also stage the board-scan distance, and the reward tail doubles the final value to index `BoardScanRewardScoreDeltaTable` before calling `AddScore`. |
| `$C6A3-$C6A6` | `PIECE_DISPLAY_STATES` | High | Four display-state bytes built by `BuildPieceDisplayStatesForCount`, scanned by `BuildPieceDisplayObjectSlotsLoop` / `InitActivePieceDisplayObject` to emit piece-display sprite objects in slots 1-4, and optionally forced by the first/all-state forced display helpers. |
| `$C696` | `PIECE_FALL_TIMER` | High | Decremented by `UpdatePieceFallTimer`, reloaded by `ReloadPieceFallTimer`, or held at `PIECE_FALL_TIMER_ANIM_HOLD_RELOAD` while drop/cursor animation is active; `UpdateFallingPieceMotionAndLanding` returns without moving the staged piece while it is nonzero. |
| `$C6A7` | `PIECE_FALL_DELAY` | High | Reload value for `PIECE_FALL_TIMER`, initialized from `GetLevelFallDelay` or `GAME_TURN_DELAY` and periodically lowered by `UpdateFallAcceleration` down to `PIECE_FALL_DELAY_MIN`. |
| `$C6A9` | `GAME_TURN_TABLE_INDEX` | High | `InitGameTurnPieceDisplay` seeds this from `GameTurnLevelStartIndexTable`; `LoadGameTurnPieceDisplayStep` indexes `GameTurnParamTable + index * GAME_TURN_PARAM_RECORD_SIZE` through `GAME_TURN_PARAM_RECORD_SHIFT`. `AdvanceGameTurnTableIndex` leaves `GAME_TURN_TABLE_INDEX_SENTINEL` unchanged, otherwise increments the index or wraps `GAME_TURN_TABLE_LOOP_END_INDEX` to `GAME_TURN_TABLE_LOOP_RESTART_INDEX`. |
| `$C6AA` | `GAME_TURN_STEP_TIMER` | High | Reloaded from the first byte of the current `GameTurnParamTable` record, decremented by `UpdateGameTurnPieceDisplay`, and advances the table when it reaches zero. |
| `$C6AB` | `RESULT_FLOW_ACTIVE` | High | Set by `ProcessRoundResultAndEnterRoundEnd` and `QueueRoundResult`, cleared to `RESULT_FLOW_INACTIVE` by `ClearRoundLandingAndResultState` and the return-to-title path, and read by Bank 1 `DrawGameplayBgTopRowIfNoResultFlow` to suppress normal field background setup during result/result-record flow. |
| `$C6AC` | `GAME_TURN_DELAY` | High | Loaded from `GAME_TURN_PARAM_FALL_DELAY_OFFSET`, the third byte of the current `GameTurnParamTable` record, optionally halved by `ACTIVE_SPEED`, then copied into `PIECE_FALL_DELAY` / `PIECE_FALL_TIMER`. |
| `$C6AD` | `PIECE_DISPLAY_FORCE_ALL_STATES_FLAG` | High | Set to `PIECE_DISPLAY_FORCE_FLAG_ACTIVE` by a timer-gated `SelectPieceDisplayCode` branch; `ApplyAllForcedPieceDisplayStatesLoop` rewrites every nonzero display-state entry to `PIECE_DISPLAY_FORCED_STATE`, and `BuildPieceDisplayStatesForCount` clears the flag to `PIECE_DISPLAY_FORCE_FLAG_INACTIVE` afterward. |
| `$C6AE` | `UNRESOLVED_LANDING_RESET_BYTE_1` | Low | Same confirmed write pattern as `UNRESOLVED_LANDING_RESET_BYTE_0`: cleared by `ClearRoundLandingAndResultState`, then cleared by `HandleMatchedLandingScanState` when the landing/scan counter reaches zero. |
| `$C6AF` | `PIECE_DISPLAY_BLINK_TIMER` | High | Decremented each `RunGameplayFrame`; when it expires, it reloads to `PIECE_DISPLAY_BLINK_PERIOD`, `ScanPieceDisplayBlinkSlotsLoop` visits `PIECE_DISPLAY_BLINK_SLOT_COUNT` display sprite slots, and `TogglePieceDisplayFrame` toggles `PIECE_DISPLAY_BLINK_FRAME_TOGGLE_MASK` except for `PIECE_DISPLAY_FORCED_STATE` / `PIECE_DISPLAY_BLINK_EXEMPT_STATE`. |
| `$C6B0` | `PIECE_FALL_ACCEL_TIMER` | High | Countdown reloaded with `$0A`; when it expires, `UpdateFallAcceleration` uses the low/high-level reload branches and lowers `PIECE_FALL_DELAY` by one until the minimum delay is reached. |
| `$C6B1` | `MENU_CURSOR` | High | Indexes the four option bytes from `$C6B2-$C6B5`. |
| `$C6B2` | `OPTION_GAME_TYPE` | High | Selected game type; copied into `GAME_TYPE` by `InitGameState`. |
| `$C6B3` | `OPTION_LEVEL` | High | Selected starting level; copied into `ACTIVE_LEVEL` and `PROGRESSION_LEVEL`. |
| `$C6B4` | `OPTION_SPEED` | High | Selected drop speed; copied into `ACTIVE_SPEED`. |
| `$C6B5` | `OPTION_BGM` | High | Selected BGM; mapped to sound commands by `ApplyGameSettings`. |
| `$C6B6` | `TWO_PLAYER_FLAG` | High | Highest-frequency named WRAM flag; used across title, options, gameplay, link, and drawing paths. |
| `$C6B7` | `ACTIVE_LEVEL` | High | Active level copied from options or link settings; used by thresholds and result/rank paths. |
| `$C6B8` | `ACTIVE_SPEED` | High | Active drop speed copied from options or link settings; used by timing and display paths, including `DrawPlayfieldSpeedValue`. |
| `$C6B9` | `LINK_RECV` | High | Link receive byte used by serial/link logic. |
| `$C6BA` | `LINK_SEND` | High | Link send byte used by serial/link logic. |
| `$C6BB` | `LINK_ROLE` | High | Read throughout 2P/link paths; values appear to distinguish no link/master/slave. |
| `$C6BC` | `TITLE_PLAYER_MARKER_TIMER` | High | `TickTitlePlayerMarkerBlink` decrements this timer before toggling the title 1P/2P selection marker. |
| `$C6BD` | `TITLE_PLAYER_MARKER_UNUSED_DELAY` | Low | Set to `TITLE_PLAYER_MARKER_UNUSED_DELAY_INITIAL` during title reset between the title marker timer and phase bytes; no direct read has been confirmed. |
| `$C6BE` | `TITLE_PLAYER_MARKER_PHASE` | High | Selects whether `TickTitlePlayerMarkerBlink` draws the top or bottom title selection marker tiles. |
| `$C6BF` | `UNRESOLVED_LANDING_SCAN_COUNTER` | Medium | Cleared by `ClearRoundLandingAndResultState`, decremented by `RunBoardScanTriggerSequence` after its animation loop and `RunBoardScanRoundTransition`, decremented by two by `HandleMatchedLandingScanState` for staged payload `BOARD_SCAN_TARGET_PAYLOAD`, and used to decide when to clear the two unresolved landing reset bytes. |
| `$C6C0` | `UNRESOLVED_LANDING_RESET_TIMER` | Low | `ClearRoundLandingAndResultState` writes `$14` here; no consumer has been confirmed in the current source. |
| `$C6C1` | `BGM_PREVIEW_TIMER` | Medium | Set during BGM option changes and decremented by `TickBgmPreviewTimer`. |
| `$C6C2` | `BGM_PREVIEW_UNUSED_PERIOD` | Low | Stores BGM-specific values during preview setup, but no direct consumer has been found in the current source. |
| `$C6C3-$C6C6` | `FIELD_ANIM_SLOT_*_CURSOR` | High | Four table cursors for sprite object slots 11, 10, 13, and 12. `StepFieldAnimSlot*` routines index `FieldSideDeltaTable` / `FieldRowDeltaTable` until sentinel `$10`, then reset the matching cursor. |
| `$C6C7-$C6CA` | `FIELD_ANIM_SLOT_*_ACTIVE` | High | Active flags for sprite object slots 12, 11, 10, and 13. Round-complete paths set all four flags; each slot update routine tests its flag and clears it when its delta table terminates. |
| `$C6CB-$C6CE` | `FIELD_COLUMN_TIMERS` | High | Four timers tied to logical sprite object slots 10-13. `SpawnFieldColumnEffect` reloads one timer by `FALLING_PIECE_GRID_COLUMN`; `UpdateFieldTimers` decrements them and clears the matching `$C2xx` slot when a timer reaches zero. |
| `$C6CF` | `LEVEL_DISPLAY_ONES` | High | Initialized from `OPTION_LEVEL + 1` or `LINK_2P_SELECTED_LEVEL + 1`, incremented as a decimal ones digit by `AdvanceATypeLevelDisplayDigits` / `AdvanceLevelDisplayDigits`, drawn by `DrawMatchingResultStats` / `DrawLevelDisplayDigits` / `DrawPlayfieldLevelDigits`, and copied into result records. |
| `$C6D0` | `LEVEL_DISPLAY_TENS` | High | Decimal tens digit paired with `LEVEL_DISPLAY_ONES`; incremented on ones rollover, drawn to the tile before the ones digit, and copied into result records. |
| `$C6D1` | `LEVEL_DISPLAY_TICK_COUNTER` | High | `TickTitleLevelDisplayDigits` increments this divider and advances `LEVEL_DISPLAY_ONES` / `LEVEL_DISPLAY_TENS` every 10 ticks before redrawing at `TITLE_LEVEL_PREVIEW_DIGITS_COORD`; setup paths clear it through `ClearLevelDisplayTickCounter`. |
| `$C6D2` | `EGG_COUNT_UNUSED_BYTE` | Low | Cleared together with the egg counter in init/reset paths, but no direct read has been confirmed. |
| `$C6D3-$C6D5` | `EGG_COUNT_ONES` / `EGG_COUNT_TENS` / `EGG_COUNT_HUNDREDS` | High | `IncrementEggCountAndRefreshDisplay` updates these as decimal digits capped at 999; `DrawPlayfieldEggCountDigits` renders ones/tens as tile `$40+digit`, `DrawPlayfieldEggDisplay` combines the count with egg text, and result setup copies hundreds/tens/ones into `CURRENT_RESULT_DETAIL_DIGITS`. |
| `$C6D6-$C6DA` | `ROUND_TIMER_DIGITS` / `ROUND_TIMER_FRAME_COUNTER` | High | `UpdateElapsedTimers` calls `TickElapsedTimerDigits` to tick this four-digit elapsed timer through `ELAPSED_TIMER_FRAMES_PER_SECOND` (`$3C`) frames; the seconds tens digit rolls at `ELAPSED_TIMER_SECONDS_TENS_LIMIT` and the helper clamps overflow to 99:59. `DrawRoundTimerDigits` draws it at the caller-provided coordinate, `DrawPlayfieldRoundTimerDigits` fixes that coordinate to row 6/column 16, `DrawBTypeTimerHeaderAndDigits` adds the 1P B-TYPE header, and B-type title/result logic reads the leading digits. |
| `$C6DB-$C6DF` | `TOTAL_TIMER_DIGITS` / `TOTAL_TIMER_FRAME_COUNTER` | High | `UpdateElapsedTimers` calls `TickElapsedTimerDigits` to tick this second four-digit elapsed timer through the same named 60-frame divider and 99:59 clamp; `ClearTotalTimerDigitsAndResume` clears the display digits at playfield init and B-type result setup copies the four digits into the result record. |
| `$C6E0` | `FIELD_COLUMN_TILE_PATTERN_INDEX` | High | Initialized to `1` by `InitPlayerCursorObject`, decremented/incremented as left/right input moves the cursor, and shifted by `DrawFieldColumnTilePattern` to select one of `FIELD_COLUMN_TILE_PATTERN_RECORD_COUNT` records in `FieldColumnTilePatternTable`. |
| `$C6E1` | `BGM_INDEX` | Medium | Used when selecting BGM/sound. |
| `$C6E2` | `PROGRESSION_LEVEL` | High | Initialized from option/link level, incremented by `AdvanceATypeLevelDisplayDigits` / `AdvanceLevelDisplayDigits`, capped at `$13` for `LevelFallDelayTable`, and passed to `ProcessMatching` in the B-type continuation path. |
| `$C6E4-$C6E5` | `ROUND_TIMER_STOPPED` / `TOTAL_TIMER_STOPPED` | High | Completion and result setup set both flags to stop elapsed-time updates; next-round setup clears only the round timer, while new playfield setup clears both timers and both stop flags. |
| `$C6E6` | `LINK_FIELD_EVENT_PAYLOAD` | Medium | Falling-piece code builds a bit-6 field-event payload here with `LINK_FIELD_EVENT_FLAG` (`$40`) / `LINK_FIELD_EVENT_BIT`, then the round-complete path copies it into `LINK_SEND_QUEUE_0`. |
| `$C6E7` | `LINK_SEND_DROP_INPUT_LOCK` | High | `Send2PData` sets `LINK_SEND_DROP_INPUT_LOCK_ACTIVE` while calling `HandlePlayfieldInput` inside its link-send wait loop; `HandlePlayfieldInput` uses it to suppress starting a new drop while still allowing movement/input polling. |
| `$C6E8-$C6EA` | `EGG_TEXT_PULSE_FRAME` / `EGG_TEXT_PULSE_TIMER` / `EGG_TEXT_PULSE_STEPS` | High | Seeded when `IncrementEggCountAndRefreshDisplay` wraps the ones digit; `UpdateEggTextAnimation` uses these bytes to alternate `DrawEggTextFrameByIndex` frames 1 and 2 with a `$28` frame delay. |
| `$C6EB-$C6EC` | `LINK_2P_SELECTED_LEVEL` / `LINK_2P_SELECTED_SPEED` | High | The 2P option loop edits these two bytes, `Exchange2PPreplaySettings` packs them into `LINK_SEND`, and 2P setup copies them into `ACTIVE_LEVEL` / `ACTIVE_SPEED`. |
| `$C6F0` | `LINK_SETTINGS_CURSOR` | High | 2P pre-play cursor. Up/down clamp it to level row `0` or speed row `1`; left/right index `LINK_2P_SELECTED_LEVEL` / `LINK_2P_SELECTED_SPEED`. |
| `$C6F1` | `SETTINGS_BLINK_PHASE` | High | `TickSettingsBlink` toggles this bit every `$0F` frames. Selected 2P setting/result-option rows use it to alternate between normal text and blank text. |
| `$C6F2` | `SETTINGS_BLINK_TIMER` | High | Reloaded with `$0F` on setup/input; decremented by `TickSettingsBlink` before toggling `SETTINGS_BLINK_PHASE`. |
| `$C6F3-$C6F4` | `EGG_TEXT_ALT_ANIM_ACTIVE` / `EGG_TEXT_ALT_ANIM_PHASE` | High | The tens-digit wrap path enables this continuous `DrawEggTextFrameByIndex` frame 1/2 alternation; `RunTitleMenu` clears both bytes, round/playfield setup clears the phase byte, and the playing-state VBlank check toggles the phase. |
| `$C6F5-$C6F6` | `ROUND_COMPLETE_TILE_BASE_X` / `ROUND_COMPLETE_TILE_BASE_Y` | High | `ShowRoundComplete` stores each round-complete tile-group base X (`ROUND_COMPLETE_TILE_BASE_X_0..3`) and fixed `ROUND_COMPLETE_TILE_GROUP_BASE_Y` here; `ProcessRoundComplete` copies the pair into sprite object slots 10-13. |
| `$C6F7` | `PIECE_DISPLAY_FORCE_FIRST_STATE_FLAG` | High | Set to `PIECE_DISPLAY_FORCE_FLAG_ACTIVE` by a timer-gated `SelectPieceDisplayCode` branch; `FindFirstForcedPieceDisplayStateLoop` finds the first nonzero display-state entry and `StoreFirstForcedPieceDisplayState` rewrites it to `PIECE_DISPLAY_FORCED_STATE`. |
| `$C6F8` | `PIECE_DISPLAY_SKIP_SPECIAL_SELECTION_FLAG` | High | One-shot flag set to `PIECE_DISPLAY_SKIP_SPECIAL_ACTIVE` by `BuildPieceDisplayStatesForCount` when display count reaches `PIECE_DISPLAY_SKIP_SPECIAL_MIN_COUNT`; the next `SelectPieceDisplayCode` call clears it and skips the timer-gated special-selection path. |
| `$C6FA` | `LINK_PENDING_FIELD_RISE` | Medium | Incoming `LINK_FIELD_EVENT_BIT` packets add to this byte after the flag bit is cleared; `SelectTwoPlayerPieceDisplayCount` / `ConsumePendingFieldRiseForDisplayCount` consume it in chunks while adjusting `SCREEN_STATE`; `ClearLinkRoundState` clears it at round/playfield setup. |
| `$C6FB` | `LINK_UNUSED_STAGING_BYTE` | Low | Cleared by `ClearLinkRoundState`; no direct read has been confirmed in the current source. |
| `$C6FC-$C6FD` | `LINK_SEND_QUEUE_0` / `LINK_SEND_QUEUE_1` | High | `TimerTickCore` alternates between these two bytes, sends the selected byte through `LINK_SEND`, then clears that queue slot. |
| `$C6FE` | `LINK_SEND_QUEUE_INDEX` | High | Alternates modulo `LINK_SEND_QUEUE_SLOT_COUNT` between 0 and 1 to select the next link send queue slot; `ClearLinkRoundState` clears it with the queue bytes. |
| `$C6FF-$C700` | `LINK_RECV_LEVEL` / `LINK_RECV_SPEED` | High | `Exchange2PPreplaySettings` unpacks peer level/speed nibbles from `LINK_RECV`; preview/result paths read these values for peer display. |
| `$C701-$C702` | `LINK_RESULT_NONZERO_MARKS` / `LINK_RESULT_ZERO_MARKS` | High | 2P result counters cleared by `RunTitleMenu`; `UpdateLinkResultMarksAndScreen` increments the nonzero or zero counter, sets the terminal overlay when either reaches `LINK_RESULT_MARK_LIMIT`, and draws 2x2 marks from `LINK_RESULT_NONZERO_MARK_BASE` (`$C4F3`) rightward or `LINK_RESULT_ZERO_MARK_BASE` (`$C4FF`) leftward. |
| `$C703` | `RESULT_CLEAR_FLAG` | High | `UpdateGameplayObjectsAndCheckBTypeClear` / `CheckBTypeColumnClearLoop` set this when all four `COLUMN_TOP_ROWS` reach `BOARD_FALL_END_ROW`; the same `ROUND_RESULT_CODE_NONZERO` value is queued for 2P clear flow, result display checks it before drawing the clear/win message, and setup paths clear it for a new round/playfield. |
| `$C704` | `RESULT_GAME_OVER_FLAG` | High | Set with `RESULT_FLAG_SET` when falling-piece placement overflows the local field; result/link display checks it for the game-over/loss message, and tie-resolution code can clear it before queueing the final result. |
| `$C705` | `ROUND_RESULT_PENDING` | High | `QueueRoundResult` sets this flag with `RESULT_FLAG_SET` after storing the result code; Bank 1 `ProcessPending2PRoundResult` consumes it to call `ProcessRoundResultAndEnterRoundEnd`, and `Send2PData` uses it to skip normal inner-frame updates once result flow is queued. |
| `$C706` | `ROUND_RESULT_CODE` | High | `QueueRoundResult` stores the argument that Bank 1 later passes to `ProcessRoundResultAndEnterRoundEnd`; observed queued values are `ROUND_RESULT_CODE_ZERO` and `ROUND_RESULT_CODE_NONZERO`, and title/start-next-round setup clears it with `ROUND_RESULT_PENDING`. |
| `$C707` | `PAUSE_FLAG` | High | `PauseGame` and the received link-pause packet store `PAUSE_FLAG_ACTIVE`, `UnpauseGame` clears it, and the 2P pause path waits in `WaitLinkPeerUnpauseLoop` until the peer pause packet clears. |
| `$C708` | `LINK_PEER_RESULT_CODE` | High | `Exchange2PResultCode` receives the peer's `LINK_RESULT_PACKET_FLAG` packet, clears `LINK_RESULT_PACKET_BIT`, and stores the peer result code here; `ResolveResultRankPosition` compares it with the local result code during 2P result resolution. |
| `$C709-$C729` | `A_TYPE_RESULT_RECORDS` | High | Three `RESULT_RECORD_SIZE` score/egg records. `InitResultRecordsIfNeeded` initializes their heads to `RESULT_RECORD_EMPTY_HEAD`, `ProcessCurrentResultRecordAndSetupScreen` compares/inserts the staged `CURRENT_RESULT_RECORD`, and result setup renders this table for A-type. |
| `$C72A-$C74A` | `B_TYPE_RESULT_RECORDS` | High | Three `RESULT_RECORD_SIZE` score/time records with the same insertion/rendering path selected when `GAME_TYPE` is nonzero. |
| `$C74B-$C755` | `CURRENT_RESULT_RECORD` | High | Staged result record built from `RESULT_RECORD_SCORE_DIGIT_COUNT` score digits, two level display digits, and either `RESULT_RECORD_A_TYPE_DETAIL_DIGIT_COUNT` egg digits or `RESULT_RECORD_B_TYPE_DETAIL_DIGIT_COUNT` timer digits before being inserted into the A/B record table. |
| `$C756` | `RESULT_RECORDS_INIT_FLAG` | High | Prevents repeated `RESULT_RECORD_EMPTY_HEAD` seeding of the result record heads. |
| `$C757-$C75A` | `WRAM_PERSIST_MAGIC` | High | Startup magic `$C7,$8A,$29,$36`; when valid, startup selects `WRAM_CLEAR_MODE_PRESERVE_RESULT_RECORDS` and `ClearWRAMLoop` skips `$C709-$C75A` so result records survive reset. If any byte fails, `UseFullWRAMClear` selects `WRAM_CLEAR_MODE_FULL` and clears the full WRAM range. |
| `$C75B-$C75C` | `ROUND_END_WAIT_TIMER` | High | Seeded with `ROUND_END_WAIT_INITIAL_FRAMES` / `ROUND_END_WAIT_INITIAL_FRAMES_HI` by `ProcessRoundResultAndEnterRoundEnd`; decremented by the 2P round-end path before continuing. |
| `$C75D` | `DROP_ANIM_ACTIVE` | High | `StartDropColumnSwapAnimation` sets it to `$FF`, `AnimateDropping` and `HandlePlayfieldInput` gate on it, and the cascade completion path clears it. |
| `$C75E` | `DROP_ANIM_FRAME_TIMER` | High | Decremented by `AnimateDropping` and reloaded before advancing drop cascade states. |
| `$C761` | `DROP_ANIM_COLUMN` | High | Stores the selected column index for collision checks, grid-position calculation, and final `$C66A` column-state swap. |
| `$C762` | `DROP_ANIM_UNUSED_GRID_ROW_TMP` | Low | Written by `CalcGridPosition` as local scratch, but no direct read has been confirmed in the current source. |
| `$C764/$C774` | `DROP_ANIM_DOWN_STATES` / `DROP_ANIM_UP_STATES` | High | Two seven-entry, two-byte-stride cascade state arrays advanced by `AnimateDropDown` and `AnimateDropUp`; entries move from `DROP_ANIM_STATE_START` through `DROP_ANIM_STATE_END`, then back to `DROP_ANIM_STATE_INACTIVE`. |
| `$C7A4` | `COLUMN_BLINK_GLOBAL_TIMER` | High | `UpdateColumnBlinkState` increments this timer and wraps it at `$30` before scanning the four column blink slots. |
| `$C7A5-$C7A8` | `COLUMN_BLINK_SLOT_TIMERS` | High | Four per-slot blink counters walked in parallel with `COLUMN_BLINK_SLOT_FLAGS`; a nonzero counter increments until `$10`, then toggles the slot frame. |
| `$C7A9-$C7AC` | `COLUMN_BLINK_SLOT_FLAGS` | High | Four active/frame bytes. `InitBlinkState` sets them to 1, title init clears them, and `UpdateColumnBlinkState` toggles active bytes between 1 and 2 before calling `DrawColumnSprite`. |
| `$C7AD` | `RESULT_RANK_POSITION` | High | `ProcessRoundResultAndEnterRoundEnd` stores the rank/high-score position returned by `ResolveResultRankPosition`; `RESULT_RANK_NONE` (`0`) selects the no-rank path, equal 2P master-side results return `RESULT_RANK_FIRST_PLACE`, and `DrawScoreRanking` / B-game round-end branching read the stored value. |
| `$C7AE-$C7CD` | `COUNTDOWN_DIGIT_BUFFER_0..3` | High | Four 8-byte digit bitmap staging buffers. `UpdateCountdownTimer` builds them from `SCORE_BCD_*` and `CountdownDigitPatternTable`; `RandomNext` blits pairs to `COUNTDOWN_BLIT_DEST_PHASE0` (`$9020`) / `COUNTDOWN_BLIT_DEST_PHASE1` (`$9120`). |
| `$C7CE` | `COUNTDOWN_BLIT_TIMER` | High | Nonzero while countdown digit buffers still need VRAM blits. `Draw1PCountdownDigitTileSlots` seeds it with `COUNTDOWN_BLIT_TIMER_RELOAD`, `RandomNext` decrements it, and result setup clears it. |
| `$C7CF` | `COUNTDOWN_BLIT_PHASE` | High | Toggled by `UpdateCountdownTimer`; selects which digit-buffer pair `RandomNext` copies to VRAM. |

## High-Priority Unnamed Regions

These addresses appear often enough to deserve early recovery. Some are real structures; some are code/data disassembly artifacts.

| Address/Range | Evidence | Initial hypothesis |
|---------------|----------|--------------------|
| `$C000-$C0ED` | Many references from Bank 1 sound routines and tables; now named as `SOUND_*` constants. | Sound engine state, channel sequence pointers, timers, pitch slide state, tempo, and wave selectors. Continue refining medium-confidence field names. |
| `$C200-$C2FF` | `SPRITE_OBJECTS`; 16 logical sprite object slots, `$10` bytes each. | `UpdateSprites` scans this page in `$10`-byte steps and expands active slots into shadow OAM; `UpdateSpriteObject` stages gameplay slots 1-4 through `$C68C-$C695`. |
| `$C300-$C3FF` | Sprite/field-adjacent work area, not yet fully mapped. | Some references may still be from real field/UI state or from older data artifacts; needs separate trace. |
| `$C400-$C49F` | `SHADOW_OAM`; 40 hardware OAM entries. | `ClearOAM` clears `$A0` bytes through `ClearShadowOamLoop`; `HideAllSprites` writes `OAM_HIDDEN_Y` to each entry Y byte through `HideShadowOamSpritesLoop`; HRAM OAM DMA copies page `$C4` to hardware OAM; `UpdateSprites` appends entries here and hides unused entries only through offset `$98`, leaving `SHADOW_OAM_MANUAL_PAIR` (`$C498`) for the direct two-sprite animation path. The adjacent `$C4A0-$C607` range is `BG_MAP_SHADOW`, not OAM. |
| `$C4A0-$C607` | Now named `BG_MAP_SHADOW`; used by result/title/2P display routines as tilemap destinations before Bank 1 copies three six-row slices to VRAM. | BG map shadow, not OAM. Continue refining named offsets inside the buffer. |

## Code/Data Misclassification Candidates

The reference scan intentionally reports address-like operands even when they occur in suspicious code. These are high-value cleanup targets because they can hide real tables.

| Area | Evidence | Action |
|------|----------|--------|
| `Yoshi/bank_000.asm` `00:$0B8D-$0ED4` | `InitGameTurnPieceDisplay` and `LoadGameTurnPieceDisplayStep` index `$0B8D + GAME_TURN_TABLE_INDEX * 4`; the head was previously decoded as bogus instructions while the tail was already `db`. | Converted the fake-code head to `GameTurnParamTable` rows and named the local index/timer/delay bytes. |
| `Yoshi/bank_000.asm` `00:$117C-$11EF` | Matching/result code copies `$117C/$1184/$1194` to shadow OAM, indexes `$119C` as `STATE_TRANSITION * 2` score pairs, and indexes `$11D4` as a tile-base table. | Converted to three OAM templates, `MatchingScoreBonusTable`, and `MatchingTileBaseIndexTable`; preserved the real-looking `$11F0` helper as code. |
| `Yoshi/bank_001.asm` `01:$40A0-$42F4` | `UpdateSprites` indexes `$40A0` as a pointer table; the range before `Draw1PCountdownDigitTileSlots` was previously decoded as bogus instructions. | Converted to `SpriteUpdatePointerTable`, object frame tables, tile-id lists, and layout triples in source. |
| `Yoshi/bank_001.asm` `01:$442C-$445B` | `DrawFieldColumnTilePattern` indexes `$442C` as three 16-byte records before real code at `$445C`. | Converted to `FieldColumnTilePatternTable`; `$445C` is now the real `StartNextRound` entry. |
| `Yoshi/bank_001.asm` `01:$462B-$465C` | `DrawEggTextFrameByIndex` selects `$462B`, `$4639`, or `$464B`, then calls `DrawStringToGrid` four times. | Converted to `EggTextFrame0..2TileRows` tile-string blocks; `$465D` remains the real `DrawPlayfieldEggCountDigits` entry. |
| `Yoshi/bank_001.asm` `01:$46ED-$46FE` | `DrawTitleLabels` draws `$46ED` and `$46F6` through `DrawStringToGrid` at row/column pairs `$0F06` and `$1006`. | Converted to `TitleLabelTextPlayer` and `TitleLabelTextYoshi`; `$46FF` remains the real `ProcessTitleInput` entry. |
| `Yoshi/bank_001.asm` `01:$55E2-$5668` | `SoundEngine` and `SoundLookupIndex` jump to `$55E2`; the bytes form a coherent sound setup routine. | Converted to `StartSoundSequence` code. |
| `Yoshi/bank_001.asm` `01:$5669-$5699` | Sound routines directly index `$566A`, `$5672`, `$567A`, and `$5682`; `$569A` is a sequence entry target, not pitch-table data. | Split into small sound support tables. |
| `Yoshi/bank_001.asm` `01:$569A-$5FE2` | Already represented as `db`, but was mostly one large unlabeled sequence block. | Added first-pass internal pointer-target labels. |
| `Yoshi/bank_001.asm` `01:$5FE3-$7C01` | Previously contained many instruction-looking bytes and fake labels. | Converted to `MusicSequenceData_*` blocks while preserving the real `$7C02` helper. |
| `Yoshi/bank_000.asm` `00:$22CC-$234B` | Field drawing routines index `$22CC` and `$230F` with `GetArrayElement`; `$234C` is called from Bank 0 and Bank 1 as code. | Converted to `FieldSideDeltaTable` and `FieldRowDeltaTable`; restored `UpdateFieldTimers` at `$234C`. |
| `Yoshi/bank_000.asm` `00:$3839-$3FFF` | `BuildRoundCompleteSummaryScreen` copies `$3839` and `$3D39` to VRAM via `VRAMCopySetup`; the following `$3E49-$3FFF` tail has no confirmed source reference and is dominated by `$39,$00` filler pairs. | Converted to `RoundCompleteSummaryGraphicTileData`, `RoundCompleteSummaryTextTileData`, and `Bank0TailPaddingData`. |

## Immediate Next Steps

1. Refine medium-confidence `SOUND_CH_*` slide/tempo/envelope names by decoding more sequence examples.
2. Trace `GAME_STATE` writes and assign concrete state names.
3. Decode the visual meaning of each Bank 2/3 graphics load range.
4. Continue refining the shared settings/pre-play cursor drawing routines.
5. Continue code/data separation for remaining small fake-code islands.
